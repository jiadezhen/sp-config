spring:
    application:
        name: item-service
    cloud:
        config:
            override-none: true

server:
    port: 8001

#配置eureka
eureka:
    client:
        service-url:
            defaultZone: http://localhost:2001/eureka


#首次更新1
